# outputs

This directory stores run artifacts generated by training scripts, such as:

- checkpoints
- logs
- confusion matrices
- prediction CSV files
- metrics summaries

The directory is kept in the public repository as a placeholder only. Generated files are ignored by default via `.gitignore`.
